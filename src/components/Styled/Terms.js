import styled from 'styled-components';

const Terms = styled.p`
    color: var(--grayishblue);
    font-size: .8rem;
    width: 90%;
    margin: 0 auto;

    a {
        color: var(--red);
        font-weight: bold;
        cursor: pointer;
    }
`;

export default Terms;